= provide(:title, "編集")
h1 編集

= render partial: 'error_message', locals: { post: @post }
= form_with model: @post, local: true, url: confirm2_post_path do |f|
  = render partial: 'form', locals: { f: f }
